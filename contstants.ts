import {User} from "./src/types/types";

export const DEFAULT_USER_ID = 213;
export const DEFAULT_USERNAME = "VLAD";
export const DEFAULT_USER: User = {
    id: DEFAULT_USER_ID,
    username: DEFAULT_USERNAME,
}